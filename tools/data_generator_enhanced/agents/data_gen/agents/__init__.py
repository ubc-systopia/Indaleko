"""
Domain-specific agents for generating different types of metadata.

This module contains the specialized agents that generate specific
types of metadata records for the Indaleko system, including:
- Storage metadata (files, directories)
- Semantic metadata (content information)
- Activity metadata (location, music, temperature)
- Relationship metadata (connections between entities)
- Machine configuration metadata (device information)
"""
